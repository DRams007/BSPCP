VITE_API_URL=http://192.168.8.118:3001

# Email Configuration using Gmail SMTP
MAIL_HOST=smtp.gmail.com
MAIL_PORT=465
MAIL_SECURE=true
MAIL_USER=bspcpemail@gmail.com
MAIL_PASS=caaruxzbwvaqnmku
MAIL_FROM="BSPCP Admin" <bspcpemail@gmail.com>

# Alternative: Try these if Gmail doesn't work
# MAIL_HOST=smtp.mailtrap.io
# MAIL_PORT=2525
# MAIL_SECURE=false
# MAIL_USER=your_mailtrap_user
# MAIL_PASS=your_mailtrap_password

BASE_URL=http://192.168.8.118:8080

# Gmail App Password Setup Instructions:
# 1. Enable 2FA on Gmail account (if not already enabled)
# 2. Go to Google Account Settings > Security > 2-Step Verification > App passwords
# 3. Generate a new app password for "Mail" and use it instead of regular password
# 4. Replace the MAIL_PASS above with the 16-character app password

# Note: If you get "Missing credentials for PLAIN" error:
# 1. Generate an App Password from Gmail account settings (https://myaccount.google.com/apppasswords)
# 2. Replace 'REPLACE_WITH_ACTUAL_APP_PASSWORD' with the 16-character app password
# 3. Ensure "Less secure app access" is enabled OR use App Passwords with 2FA
# 4. For Gmail, you may need to enable SMTP in account settings
